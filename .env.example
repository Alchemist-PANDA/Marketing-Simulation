# API Configuration (Optional)
# If integrating with external LLMs for semantic parsing or sentiment analysis.
# The core simulation engine runs 100% locally via NumPy and sentence-transformers.

# Example: If extending the Forensic Analysis module with a cloud LLM
OPENAI_API_KEY=your_api_key_here
ANTHROPIC_API_KEY=your_api_key_here

# Application Settings
DEBUG_MODE=False
PORT=8000
